import java.util.Scanner;

class Arrays {
    public static void main(String[] args) {
        // int[] rno = new int[5];
        // int[] rno2 = { 2, 3, 4, 5, 5 };

        // int[] ros;//declaration of array, ros is getting defined in the stack
        // ros=new int[5];//initialisation: actually here object is being created in the
        // memory(heap)
        // declaration happens at compile time and actual object creation happen at
        // runtime in heap i.e:
        // Dynamic memory allocation

        // Array objects are in heap
        // heap objects may not be continuous, it depends on JVM
        // there is no concept of pointers in java
        // heap area is not continous and array objects are in heap area
        // primitive data types are passed by value and non-primitive data types are
        // passed by reference
        // non-primitive

        // System.out.println(arr[0]);
        // new is used to create an object

        // for each loop
        // String[] arr = new String[4];

        // Scanner in = new Scanner(System.in);

        // for (int i = 0; i < arr.length; i++) {
        // arr[i] = in.next();
        // }
        // System.out.println(arr[0]);

        int[][] arr = {
                { 1, 2, 3, 4 },
                { 5, 6 },
                { 7, 8, 9 }
        };

        
    } 
       

    

